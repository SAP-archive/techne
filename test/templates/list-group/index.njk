{% extends "layout.njk" %}
{% from "./component.njk" import list_group %}
{% from "../button/component.njk" import button %}
{% from "../icon/component.njk" import icon %}
{% from "../form/component.njk" import form_group, form_control %}
{% from "./../format.njk" import format %}
{% set css_deps = ['helpers','components/button','components/form','icons'] %}

{% block content %}

<h1>Lists</h1>
{% set example %}
{{ list_group({ items: data.properties.items }) }}
{% endset %}
{{ format(example) }}
<br><br>

<h1>List with actions</h1>
{% set example %}
{{ list_group(data.properties) }}
{% endset %}
{{ format(example) }}
<br><br>
<h1>List with checkboxes</h1>
{% set example %}
{{ list_group({
    items: [
        form_group(
            type="checkbox",
            modifier={ item: "" },
            properties={ items: [
                { properties: {
                    id: "checkbox-1",
                    name: "checkbox-name-1",
                    label: "List item 1"
                }
            }]}),
        form_group(
            type="checkbox",
            modifier={ item: "" },
            properties={ items: [
                { properties: {
                    id: "checkbox-2",
                    name: "checkbox-name-2",
                    label: "List item 2"
                }
            }]}),
        form_group(
            type="checkbox",
            modifier={ item: "" },
            properties={ items: [
                { properties: {
                    id: "checkbox-3",
                    name: "checkbox-name-3",
                    label: "List item 3"
                }
            }]}),
            form_group(
            type="checkbox",
            modifier={ item: "" },
            properties={ items: [
                { properties: {
                    id: "checkbox-4",
                    name: "checkbox-name-4",
                    label: "List item 4 - Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud"
                }
            }]})
    ]
    })
}}
{% endset %}
{{ format(example) }}




{% endblock %}
