{% extends "layout.njk" %}
{% from "./../format.njk" import format %}
{% from "../button/component.njk" import button %}
{% from "./component.njk" import table %}

<!-- include add'tl css from src/styles/, e.g., ['helpers','components/button'] -->
{% set css_deps = ['helpers'] %}

{% block content %}

    <h1>table</h1>

    <!-- output the component example and the code snippet -->
    {% set example %}
    {{  table(data.properties)
    }}
    {% endset %}
    {{ format(example) }}

    {% set example %}
    {{  table(
        {
            "headers": ["", "Header 2", "Header 3", "Header 4", "Header 5"],
            "rows": [
            ["<input type=checkbox>", "<a class=tn-has-font-weight-semi>user.name@email.com</a>", "First Name", "Last Name", "01/26/17"]
            ]
        }
        )
    }}
    {% endset %}
    {{ format(example) }}




{% endblock %}
