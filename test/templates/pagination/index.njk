{% extends "layout.njk" %}
{% from "./../format.njk" import format %}
{% from "./component.njk" import pagination %}

<!-- include add'tl css from src/styles/, e.g., ['helpers','components/button'] -->
{% set css_deps = ['components/pagination'] %}

{% block content %}

<h1>pagination</h1>

<!-- output the component example and the code snippet -->
<h1>Page 1</h1>
{% set example %}
{{  pagination(
        properties={
            total: 10,
            number: 3,
            page: 1
        }
    )
}}
{{  pagination(
        properties={
            total: 10,
            number: 3,
            page: 2
        }
    )
}}
{{  pagination(
        properties={
            total: 500,
            number: 10,
            page: 2
        }
    )
}}


{% endset %}

{{- format(example) -}}

{% endblock %}
