{#
    see ./data.json for object structure
    FOR simplicity, this component has standalone params
    instead of more complex modifier.block options
#}
{%- macro icon(glyph="", size="") -%}
<span class="tn-icon{{ (glyph if size == "" else [glyph, size]) | modifier('icon') }}" role="presentation"></span>
{%- endmacro %}
